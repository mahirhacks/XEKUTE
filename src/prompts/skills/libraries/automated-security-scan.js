"use strict";

module.exports = `# VAPT SKILL — Automated scanning

Use scanners to produce leads, not final findings. Record the exact target set, scanner/template version, request limits, baseline, output path, and parser result. Promote a scanner hit only after a focused hypothesis, reproducible evidence, false-positive check, affected scope, and limitation are recorded.`;
