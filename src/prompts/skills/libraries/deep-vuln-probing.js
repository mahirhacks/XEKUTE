"use strict";

module.exports = `# VAPT SKILL — Deep impact analysis

When a hypothesis has supporting evidence, reproduce it with the smallest useful sequence, isolate the required identity and object, and bound the demonstrated impact. Preserve before/after evidence, affected URLs and roles, negative controls, and limitations. Stop once the claim is sufficiently supported or mark it inconclusive; never infer impact from a title or status code alone.`;
