"use strict";

module.exports = `# VAPT SKILL — Header, token, and CSRF analysis

Compare request and response headers, cookies, CSP/HSTS, frame and content-type protections, JWT claims, token rotation, and CSRF behavior. Use benign differentials and a negative control. Record route, method, identity, expected signal, actual signal, evidence ID, WSTG check ID, and whether the result is supporting, rejecting, or inconclusive. Include HPP checks when duplicate parameters are relevant.`;
