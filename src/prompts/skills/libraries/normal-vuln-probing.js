"use strict";

module.exports = `# VAPT SKILL — Normal vulnerability probing

Test a named hypothesis across access control, authentication, sessions, injection, configuration, business logic, and APIs. Link each probe to an exact request, identity, expected signal, actual observation, evidence ID, and verdict. For example, an IDOR hypothesis needs an ownership comparison; an injection hypothesis needs a controlled differential and a false-positive control.`;
