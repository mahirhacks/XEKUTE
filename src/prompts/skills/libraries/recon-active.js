"use strict";

module.exports = `# VAPT SKILL — Active recon and enumeration

Use the smallest scoped probe that resolves one inventory question: live HTTP surface, route existence, service exposure, or technology version. Keep requests bounded, preserve raw output and exit status, and stop when the observed target or redirect no longer passes the application scope check.`;
