"use strict";

module.exports = `# VAPT SKILL — Low-impact vulnerability probing

Start with one reversible benign differential for one hypothesis. Compare a baseline, a controlled variant, and a negative control. Prefer one request over bulk traffic, capture the complete exchange, and record the expected supporting/rejecting signal. Do not describe a lead as a finding without reproducible evidence.`;
